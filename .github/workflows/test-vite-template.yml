name: Test Vite Template

on:
  push:
    branches: [main]
    paths:
      - 'templates/vite/**'
      - 'tests/**'
  pull_request:
    branches: [main]
    paths:
      - 'templates/vite/**'
      - 'tests/**'

jobs:
  test-vite-template:
    uses: ./.github/workflows/test-template.yml
    with:
      template: vite
