name: Test Starter Template

on:
  push:
    branches: [main]
    paths:
      - 'templates/starter/**'
      - 'tests/**'
  pull_request:
    branches: [main]
    paths:
      - 'templates/starter/**'
      - 'tests/**'

jobs:
  test:
    uses: ./.github/workflows/test-template.yml
    with:
      template: starter
